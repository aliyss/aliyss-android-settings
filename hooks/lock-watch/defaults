# lock-watch defaults (sourced by the service run script; overridden by the
# hookConfig env file written by home-manager). Actions are deliberately empty:
# as an event source it does nothing until the dotfiles configure them.
CHECK_INTERVAL=10
LOCK_ACTION=
UNLOCK_ACTION=
ACTION=
NOTIFY=0
